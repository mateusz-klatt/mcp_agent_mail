{
  "_DEPRECATED_TEMPLATE": [
    "Do not copy this file to .claude/settings.json.",
    "MCP Agent Mail configuration and hooks are installed at user scope.",
    "From this repository, run: bash scripts/integrate_claude_code.sh",
    "Provide the MCP URL and bearer through the installer environment or ~/.agent-mail.env.",
    "The installer merges its managed entries into ~/.claude/settings.json and ~/.claude.json while preserving unrelated user configuration."
  ]
}
